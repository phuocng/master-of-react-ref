import * as React from 'react';
import { Tooltip } from './Tooltip';

export default App = () => {
    return (
        <Tooltip tip="A sample tip content">
            <div>Hover me</div>
        </Tooltip>
    );
};
